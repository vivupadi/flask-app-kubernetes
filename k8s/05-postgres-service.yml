apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: taskmanager
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  type: ClusterIP  # Internal only